<UserControl x:Class="FirstDraft.ApplyDemo.Views.ApplyHexBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FirstDraft.ApplyDemo.Views"
             xmlns:fdcontrol="clr-namespace:FirstDraft.Controls;assembly=FirstDraft"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource BaseTextBlock}"/>
    </UserControl.Resources>
    <Grid>
        <DockPanel>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <StackPanel  HorizontalAlignment="Left" Margin="5">

                            <WrapPanel>
                                <fdcontrol:HexBox Icon="{Binding hexbox, Source={StaticResource IconSet}}" Text="十六进制输入文本框" Margin="5"/>
                                <fdcontrol:HexBox IconSize="0" Text="6B636F44" Width="200" Margin="5"/>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_byte, Source={StaticResource IconSet}}" Text="442E6C65 3D6B636F" HexMode="BYTE" Tag="BYTE" Width="200" Margin="5"/>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_uint16, Source={StaticResource IconSet}}" Text="442E6C65 3D6B636F" HexMode="UINT16" Tag="UINT16" Width="200" Margin="5"/>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_uint32, Source={StaticResource IconSet}}" Text="442E6C65 3D6B636F" HexMode="UINT32" Tag="UINT32" Width="200" Margin="5"/>
                            </WrapPanel>


                        </StackPanel>
                    </Border>




                    <TextBlock Text="默认样式：" FontWeight="Black" Margin="0 10 0 0"/>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="图标文本框默认提供一个" />
                        <Run Text="Icon" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="属性，可供用户设置文本框指代信息的图标。" />
                    </TextBlock>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <StackPanel  HorizontalAlignment="Left" Margin="5">
                            <WrapPanel>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_byte, Source={StaticResource IconSet}}" Text="6B636F44" Width="200" Margin="5"/>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_byte, Source={StaticResource IconSet}}" Text="8DFF" Width="200" Margin="5" BorderThickness="0 0 0 1" />
                                <fdcontrol:HexBox Icon="{Binding hex_converter_byte, Source={StaticResource IconSet}}" Text="8dff" Width="200" Margin="5" IsEnabled="False"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="如果取消Icon可以通过设置" />
                        <Run Text="IconSize" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text=" = 0" />
                    </TextBlock>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <StackPanel  HorizontalAlignment="Left" Margin="5">
                            <WrapPanel>
                                <fdcontrol:HexBox IconSize="0" Text="6B636F44" Width="200" Margin="5"/>
                                <fdcontrol:HexBox IconSize="0" Text="8DFF" Width="200" Margin="5" BorderThickness="0 0 0 1" />
                                <fdcontrol:HexBox IconSize="0" Text="8dff" Width="200" Margin="5" IsEnabled="False"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="可以设置" />
                        <Run Text="Tag" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="属性，可供用户设置文本框输入数值前文本提示" />
                    </TextBlock>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <StackPanel  HorizontalAlignment="Left" Margin="5">
                            <WrapPanel>
                                <fdcontrol:HexBox Icon="{Binding search, Source={StaticResource IconSet}}" Text="6B636F44" Tag="搜索" Width="200" Margin="5"/>
                                <fdcontrol:HexBox Icon="{Binding chip, Source={StaticResource IconSet}}" Text="6B636F44" Tag="芯片首地址" Width="200" Margin="5" BorderThickness="0 0 0 1" />
                                <fdcontrol:HexBox Icon="{Binding chip, Source={StaticResource IconSet}}" Text="6B636F44" Tag="芯片首地址" Width="200" Margin="5" BorderThickness="0 0 0 1" IsEnabled="False"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                     <Run Text="可以设置" />
                     <Run Text="HexMode" FontWeight="Black"  FontStyle="Oblique"/>
                     <Run Text="属性，可以设置按照BYTE(单字节)、UINT16（双字节）、UINT32（4字节）划分显示。" />
                     <Run Text="默认是单字节显示" />
                    </TextBlock>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <StackPanel  HorizontalAlignment="Left" Margin="5">
                            <WrapPanel>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_byte, Source={StaticResource IconSet}}" HexMode="BYTE" Text="442E6C65 3D6B636F" Tag="BYTE" Width="200" Margin="5"/>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_uint16, Source={StaticResource IconSet}}" HexMode="UINT16" Text="442E6C65 3D6B636F" Tag="UINT16" Width="200" Margin="5"/>
                                <fdcontrol:HexBox Icon="{Binding hex_converter_uint32, Source={StaticResource IconSet}}" HexMode="UINT32" Text="442E6C65 3D6B636F" Tag="UINT32" Width="200" Margin="5"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="二进制文件查看" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <Grid>
                            <WrapPanel>
                            </WrapPanel>
                            <fdcontrol:HexBox Text="" IconSize="0"
                                              HorizontalAlignment="Stretch"
                                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                                              Name="fileHexBox"
                                              TextWrapping="Wrap"
                                              Height="200"
                                              Padding="0 8 0 0"
                                              VerticalAlignment="Top"
                                              Margin="5"/>
                            <WrapPanel  HorizontalAlignment="Left" VerticalAlignment="Top">
                                <Button Style="{DynamicResource Button.NoBackground}" Margin="8 5 5 5" Content="请选择要查看的文件" Click="OpenFile_Click"/>
                                <Button Style="{DynamicResource Button.NoBackground}" Margin="8 5 5 5" Content="清除" Foreground="OrangeRed" Click="Clear_Click"/>
                            </WrapPanel>
                        </Grid>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>