<fdc:AnimatableUserControl x:Class="FirstDraft.ApplyDemo.Views.ApplyButtonView"
                                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                                 xmlns:local="clr-namespace:FirstDraft.ApplyDemo.Views"
                                  xmlns:fdattprop="clr-namespace:FirstDraft.AttachedProperties;assembly=FirstDraft"
                                  DataContext="{Binding ApplyButtonViewModel, Source={StaticResource Locator}}"
                                 xmlns:fdc="clr-namespace:FirstDraft.Controls;assembly=FirstDraft"
                                 mc:Ignorable="d" 
                                 d:DesignHeight="450" d:DesignWidth="800">
    <fdc:AnimatableUserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource BaseTextBlock}"/>
    </fdc:AnimatableUserControl.Resources>
    <Grid>
        <DockPanel>
            <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <Button Margin="5" Content="按钮" />
                            <Button Margin="5" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}"/>
                            <Button Margin="5" fdc:IconBlock.Icon="{Binding rect_fill, Source={StaticResource IconSet}}" Content="图标按钮"/>
                            <Button Margin="5" fdc:IconBlock.Icon="{Binding rect_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Fill="SandyBrown" Content="图标按钮"/>

                            <Button Margin="5" Content="用户" BorderBrush="{DynamicResource Button.Static.Border}" fdc:BoderAtt.CornerRadius="3" BorderThickness="1"/>
                            <Button Margin="5" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="3" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                            <Button Margin="5" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="3" Content="用户" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>

                            <Button Margin="5" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="12" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="12" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" />
                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}"  Content="邮件"/>

                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" Content="按钮" />
                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding rect_fill, Source={StaticResource IconSet}}" Content="图标按钮"/>
                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}"/>
                        </WrapPanel>
                    </Border>


                    <TextBlock Text="默认样式：" FontWeight="Black" Margin="0 10 0 0"/>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                       <Run Text="无背景、带边框，" />
                       <Run Text="鼠标放上去显示背景、切换前景色的纯按钮" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="可以通过设置" />
                        <Run Text="Content" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="显示文本等信息。" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="新增附加属性" />
                        <Run Text="IconBlock.Icon" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="添加图标显示。（使用时注意命名空间添加）" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                       <Run Text="新增附加属性" />
                       <Run Text="IconBlock.Size" FontWeight="Black"  FontStyle="Oblique"/>
                       <Run Text="设置图标大小。（默认大小16px）" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="新增附加属性" />
                        <Run Text="IconBlock.Fill" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="设置图标颜色。（默认颜色与字体保持一致）" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="新增附加属性" />
                        <Run Text="BoderAtt.CornerRadius" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="设置按钮圆角。（默认无圆角，使用时需要设置" />
                        <Run Text="BorderThickness" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="BorderBrush" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="才可以看到边框）" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Border BorderThickness="1" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Content="首页" />
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}"/>
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}" Content="首页"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Content="设置" FontSize="14"/>
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" />
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" FontSize="14" Content="设置" />
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Content="邮件" BorderBrush="{DynamicResource Button.Static.Border}" fdc:BoderAtt.CornerRadius="3" BorderThickness="1"/>
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="12" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                                            <Button Margin="5" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="3" Content="邮件" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Button Margin="5" fdc:IconBlock.Icon="{Binding cube_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" fdc:IconBlock.Fill="SeaGreen" Content="收藏" />
                                <Button Margin="5" IsEnabled="False" fdc:IconBlock.Icon="{Binding infomation_fill, Source={StaticResource IconSet}}" Content="信息" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>


                    <TextBlock Text="预定义样式：" FontWeight="Black" Margin="0 10 0 0"/>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="Button.Opacity" FontWeight="Black" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                         <Run Text="通过设置" />
                         <Run Text="Style" FontWeight="Black"  FontStyle="Oblique"/>
                         <Run Text="为" />
                         <Run Text="Button.Opacity" FontWeight="Black"  FontStyle="Oblique"/>
                         <Run Text="  无背景、带边框，" />
                         <Run Text="鼠标放上去修改透明度的纯按钮" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Border BorderThickness="1" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" Content="首页" />
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}" Content="首页"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" Content="设置" FontSize="14"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" />
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" FontSize="14" Content="设置" />
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" Content="邮件" BorderBrush="{DynamicResource Button.Static.Border}" fdc:BoderAtt.CornerRadius="3" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="12" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="3" Content="邮件" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Button Margin="5" Style="{DynamicResource Button.Opacity}" fdc:IconBlock.Icon="{Binding cube_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" fdc:IconBlock.Fill="SeaGreen" Content="收藏" />
                                <Button Margin="5" Style="{DynamicResource Button.Opacity}" IsEnabled="False" fdc:IconBlock.Icon="{Binding infomation_fill, Source={StaticResource IconSet}}" Content="信息" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>




                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="Button.NoBackground" FontWeight="Black" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="通过设置" />
                        <Run Text="Style" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="为" />
                        <Run Text="Button.NoBackground" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="  无背景、边框，" />
                        <Run Text="鼠标放上去修改前景色的纯按钮" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Border BorderThickness="1" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" Content="首页" />
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}"/>
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}" Content="首页"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" Content="设置" FontSize="14"/>
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" />
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" FontSize="14" Content="设置" />
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" Content="邮件" BorderBrush="{DynamicResource Button.Static.Border}" fdc:BoderAtt.CornerRadius="3" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="12" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="3" Content="邮件" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Button Margin="5" Style="{DynamicResource Button.NoBackground}" fdc:IconBlock.Icon="{Binding cube_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" fdc:IconBlock.Fill="SeaGreen" Content="收藏" />
                                <Button Margin="5" Style="{DynamicResource Button.NoBackground}" IsEnabled="False" fdc:IconBlock.Icon="{Binding infomation_fill, Source={StaticResource IconSet}}" Content="信息" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="Button.Navi.TextBold" FontWeight="Black" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                         <Run Text="通过设置" />
                         <Run Text="Style" FontWeight="Black"  FontStyle="Oblique"/>
                         <Run Text="为" />
                         <Run Text="Button.Navi.TextBold" FontWeight="Black"  FontStyle="Oblique"/>
                         <Run Text=" 不显示背景色的按钮样式。" />
                    </TextBlock>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Border BorderThickness="1" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" Content="首页" />
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}" Content="首页"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" Content="设置" FontSize="14"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" />
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" FontSize="14" Content="设置" />
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" Content="邮件" BorderBrush="{DynamicResource Button.Static.Border}" fdc:BoderAtt.CornerRadius="3" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="12" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="3" Content="邮件" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" fdc:IconBlock.Icon="{Binding cube_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" fdc:IconBlock.Fill="SeaGreen" Content="收藏" />
                                <Button Margin="5" Style="{DynamicResource Button.Navi.TextBold}" IsEnabled="False" fdc:IconBlock.Icon="{Binding infomation_fill, Source={StaticResource IconSet}}" Content="信息" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="Button.Over.Back" FontWeight="Black" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                       <Run Text="通过设置" />
                       <Run Text="Style" FontWeight="Black"  FontStyle="Oblique"/>
                       <Run Text="为" />
                       <Run Text="Button.Over.Back" FontWeight="Black"  FontStyle="Oblique"/>
                       <Run Text=" 鼠标覆盖上去显示背景色的按钮样式。" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Border BorderThickness="1" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" Content="首页" />
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding home_fill, Source={StaticResource IconSet}}" Content="首页"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" Content="设置" FontSize="14"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" />
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding settings_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" FontSize="14" Content="设置" />
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" BorderBrush="Transparent" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding user_fill, Source={StaticResource IconSet}}" Content="用户" BorderBrush="Transparent" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Border BorderThickness="1" Margin="5 0 0 0" BorderBrush="Gainsboro">
                                    <WrapPanel  Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" Content="邮件" BorderBrush="{DynamicResource Button.Static.Border}" fdc:BoderAtt.CornerRadius="3" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="12" BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"/>
                                            <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding email_fill, Source={StaticResource IconSet}}" fdc:BoderAtt.CornerRadius="3" Content="邮件" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>
                                <Button Margin="5" Style="{DynamicResource Button.Over.Back}" fdc:IconBlock.Icon="{Binding cube_fill, Source={StaticResource IconSet}}" fdc:IconBlock.Size="16" fdc:IconBlock.Fill="SeaGreen" Content="收藏" />
                                <Button Margin="5" Style="{DynamicResource Button.Over.Back}" IsEnabled="False" fdc:IconBlock.Icon="{Binding infomation_fill, Source={StaticResource IconSet}}" Content="信息" BorderBrush="{DynamicResource Accent}" BorderThickness="1"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>


                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="Button.Navi.TextBold" FontWeight="Black" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="通过设置" />
                        <Run Text="Style" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="为" />
                        <Run Text="Button.Navi.TextBold" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text=" 不显示背景色的按钮样式。" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{DynamicResource Button.Navi.TextBold}" Margin="5" Content="使能"/>
                                <Button Style="{DynamicResource Button.Navi.TextBold}" Margin="5" Content="不使能" IsEnabled="False"/>
                                <Button Style="{DynamicResource Button.Navi.TextBold}" Margin="5" Content="点击查看详情" Foreground="Brown"/>
                            </StackPanel>

                        </WrapPanel>
                    </Border>


                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="Button.Over.Back" FontWeight="Black" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                         <Run Text="通过设置" />
                         <Run Text="Style" FontWeight="Black"  FontStyle="Oblique"/>
                         <Run Text="为" />
                         <Run Text="Button.Over.Back" FontWeight="Black"  FontStyle="Oblique"/>
                         <Run Text=" 鼠标覆盖上去显示背景色的按钮样式。" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{DynamicResource Button.Over.Back}" Margin="5" Content="使能"/>
                                <Button Style="{DynamicResource Button.Over.Back}" Margin="5" Content="不使能" IsEnabled="False"/>
                                <Button Style="{DynamicResource Button.Over.Back}" Margin="5" Content="点击查看详情" Foreground="Brown"/>
                            </StackPanel>

                        </WrapPanel>
                    </Border>

                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="Button.Spin" FontWeight="Black" />
                    </TextBlock>
                    <TextBlock Padding="10 0 0 0" TextWrapping="Wrap">
                        <Run Text="通过设置" />
                        <Run Text="Style" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="为" />
                        <Run Text="Button.Spin" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text=" 并且添加附加属性" />
                        <Run Text="FirstDraft.AttachedProperties"  FontStyle="Oblique"/>
                        <Run Text="ButtonAttProp.IsRunning" FontWeight="Black"  FontStyle="Oblique"/>
                        <Run Text="与ViewModel的属性绑定起来结合使用，可以在长时间执行过程中显示等待动画，直到用户设置结束。最好采用异步方式调用执行过程。" />
                    </TextBlock>

                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="登录" Margin="5" fdattprop:ButtonAttProp.IsRunning="{Binding IsRunning}" Command="{Binding SignCommand}" Style="{DynamicResource Button.Spin}"/>
                            </StackPanel>
                        </WrapPanel>
                    </Border>

                </StackPanel>

            </ScrollViewer>

        </DockPanel>
    </Grid>
</fdc:AnimatableUserControl>