<UserControl x:Class="FirstDraft.ApplyDemo.Views.ApplyButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FirstDraft.ApplyDemo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border BorderThickness="1" BorderBrush="Gainsboro">
                        <WrapPanel  Margin="5">

                            <Button Margin="5" Content="按钮"/>
                            <Button Margin="5" Content="导入"/>
                            <Button Margin="5" Content="不使能" IsEnabled="False"/>

                            <Button Margin="5" Content="Button"/>
                            <Button Margin="5" Content="Import"/>
                            <Button Margin="5" Content="Not enabled" IsEnabled="False"/>
                        </WrapPanel>
                    </Border>

                </StackPanel>

            </ScrollViewer>

        </DockPanel>
    </Grid>
</UserControl>
