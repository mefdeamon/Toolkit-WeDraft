<UserControl x:Class="FirstDraft.ApplyDemo.WXWork.Views.MeetingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FirstDraft.ApplyDemo.WXWork.Views"
             mc:Ignorable="d" 
             DataContext="{Binding MeetingViewModel, Source={StaticResource Locator}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Frame DataContext="{Binding Current}" Content="{Binding Content}"  FocusVisualStyle="{x:Null}" NavigationUIVisibility="Hidden"/>
    </Grid>
</UserControl>
