<UserControl x:Class="FirstDraft.ApplyDemo.Parsec.Views.ComputersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FirstDraft.ApplyDemo.Parsec.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource BaseTextBlock}"/>
    </UserControl.Resources>
    <Grid>
        <DockPanel Margin="70 0">
            <DockPanel DockPanel.Dock="Top" >
                <TextBlock Text="Computers" FontWeight="Light" FontSize="45" DockPanel.Dock="Top"/>
                <DockPanel>
                    <TextBlock Text="Connect to your computer or a friend's computer in low latency desktop mode." FontSize="14" Margin="10"/>
                </DockPanel>
            </DockPanel>
            <Rectangle Height="3" Opacity="0.1" Fill="White" DockPanel.Dock="Top"/>

            <TextBlock/>
        </DockPanel>
    </Grid>
</UserControl>
