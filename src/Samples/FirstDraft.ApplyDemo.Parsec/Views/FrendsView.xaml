<UserControl x:Class="FirstDraft.ApplyDemo.Parsec.Views.FrendsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FirstDraft.ApplyDemo.Parsec.Views"
             xmlns:fdcontrols="clr-namespace:FirstDraft.Controls;assembly=FirstDraft"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource MediumDark}">
        <DockPanel MinWidth="100">
            <fdcontrols:IconButton DockPanel.Dock="Top" CornerRadius="5" Margin="0 5" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Icon="{Binding ipaddressbox, Source={StaticResource IconSet}}" Content="Add Frend"/>
            <fdcontrols:IconButton DockPanel.Dock="Top" CornerRadius="5" Margin="0 5" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Icon="{Binding user_fill, Source={StaticResource IconSet}}" Content="View Frend Requests (0)"/>
            <TextBlock Text="FRENDS" Margin="0 5"/>
        </DockPanel>
    </Grid>
</UserControl>
